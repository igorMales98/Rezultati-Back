schema {
    query: ZemljaQuery
}

type ZemljaQuery {
    allZemlja: [Zemlja]
    zemlja(id: ID): Zemlja
}

type Zemlja {
    id: String
    naziv: String
    lige: [Liga]
}

type Liga {
    id: String
    naziv: String
}
